# 内置函数id()可返回一个对象的标识，返回值为整数。这个整数通常对应该对象在内存中的位置

def compare(a, b):
	if a is b:
		statements
	if a ==b:
		statements
	if type(a) is type(b):
		statements

# 检查对象的类型内置函数isinstance(object, type)
if isinstance(s, list):
	s.append(item)
if isinstance(s, dict):
	d.update(t)

	